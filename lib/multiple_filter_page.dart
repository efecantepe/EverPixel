import 'package:flutter/material.dart';

class MultipleFilterPage extends StatelessWidget {
  const MultipleFilterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("efe")),
    );
  }
}
